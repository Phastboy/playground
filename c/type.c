#include <stdio.h>

int main(int argc, char *argv[])
{
    int x, y;

    x=7;
    y=5;

    printf("if x=%d y=%d\n", x, y);
    printf("x/y equals %d\n", x/y);
    printf("(float) x/y  equals %f\n", (float)x/y);
    return 0;
}
